--- origsrc/WebKit-r39090/JavaScriptCore/API/JSStringRef.h	2008-11-24 07:35:23.000000000 -0600
+++ src/WebKit-r39090/JavaScriptCore/API/JSStringRef.h	2008-12-10 22:36:28.206125000 -0600
@@ -37,7 +37,7 @@
 extern "C" {
 #endif
 
-#if !defined(WIN32) && !defined(_WIN32)
+#if !defined(WIN32) && !defined(_WIN32) && !defined(__CYGWIN__)
 /*!
 @typedef JSChar
 @abstract A Unicode character.
--- origsrc/WebKit-r39090/JavaScriptCore/runtime/DateMath.h	2008-11-24 07:35:25.000000000 -0600
+++ src/WebKit-r39090/JavaScriptCore/runtime/DateMath.h	2008-12-10 22:29:41.378000000 -0600
@@ -113,7 +113,7 @@
         , year(inTm.tm_year)
         , isDST(inTm.tm_isdst)
     {
-#if !PLATFORM(WIN_OS) && !PLATFORM(SOLARIS) && !COMPILER(RVCT)
+#if !PLATFORM(WIN_OS) && !PLATFORM(SOLARIS) && !PLATFORM(CYGWIN) && !COMPILER(RVCT)
         utcOffset = static_cast<int>(inTm.tm_gmtoff);
 
         int inZoneSize = strlen(inTm.tm_zone) + 1;
@@ -140,7 +140,7 @@
         ret.tm_year  =  year;
         ret.tm_isdst =  isDST;
 
-#if !PLATFORM(WIN_OS) && !PLATFORM(SOLARIS) && !COMPILER(RVCT)
+#if !PLATFORM(WIN_OS) && !PLATFORM(SOLARIS) && !PLATFORM(CYGWIN) && !COMPILER(RVCT)
         ret.tm_gmtoff = static_cast<long>(utcOffset);
         ret.tm_zone = timeZone;
 #endif
--- origsrc/WebKit-r39090/JavaScriptCore/wtf/Platform.h	2008-12-04 22:38:13.000000000 -0600
+++ src/WebKit-r39090/JavaScriptCore/wtf/Platform.h	2008-12-10 22:28:31.534250000 -0600
@@ -42,6 +42,13 @@
 #define WTF_PLATFORM_DARWIN 1
 #endif
 
+/* PLATFORM(CYGWIN) */
+/* Operating system level dependencies for Cygwin that should */
+/* be used regardless of operating environment */
+#ifdef __CYGWIN__
+#define WTF_PLATFORM_CYGWIN 1
+#endif
+
 /* PLATFORM(WIN_OS) */
 /* Operating system level dependencies for Windows that should be used */
 /* regardless of operating environment */
@@ -91,6 +98,7 @@
 /* Operating system level dependencies for Unix-like systems that */
 /* should be used regardless of operating environment */
 #if   PLATFORM(DARWIN)     \
+   || PLATFORM(CYGWIN)     \
    || PLATFORM(FREEBSD)    \
    || PLATFORM(S60)        \
    || defined(unix)        \
--- origsrc/WebKit-r39090/WebKitTools/DumpRenderTree/DumpRenderTree.h	2008-11-24 07:36:57.000000000 -0600
+++ src/WebKit-r39090/WebKitTools/DumpRenderTree/DumpRenderTree.h	2008-12-10 22:44:05.190500000 -0600
@@ -49,7 +49,7 @@
 
 #include <string>
 
-#if !PLATFORM(OPENBSD)
+#if !PLATFORM(OPENBSD) && !PLATFORM(CYGWIN)
 std::wstring urlSuitableForTestResult(const std::wstring& url);
 #endif
 
